{
  "name": "mat-textile-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "admin-install": "cd admin && npm install",
    "admin-build": "cd admin && VITE_ADMIN_SUBPATH=true npm run build",
    "frontend-install": "cd frontend && npm install",
    "frontend-build": "cd frontend && npm run build",
    "build": "npm run admin-install && npm run admin-build && npm run frontend-install && npm run frontend-build && rm -rf dist && mkdir -p dist && cp -r frontend/dist/* dist/ && mkdir -p dist/admin && cp -r admin/dist/* dist/admin/"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "google-auth-library": "^10.5.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.1.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.15",
    "pdfkit": "^0.17.2",
    "stripe": "^14.17.0",
    "validator": "^13.11.0",
    "xlsx": "^0.18.5"
  }
}
